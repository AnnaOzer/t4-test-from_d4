view - специальный объект, обслуживающий представление
$this->view доступен в любом контроллере, хотя смысла в этом немного

$view->addTemplatePath($path)
сообщаем о ддополнительном пути, где икать шаблоны, кроме стандартных
ищется сначала в лейаутах, потом в темплейтах,
потом в добавленных
// два слеша означают папка protected
отдельно хранятя шаблоны для писем
урок 6 18 минута - просмотр проекта чессес

$view->render($template, $data) возвращает отрендеренный шаблон,
то есть куда уже вставлены данные
делает ретурн
$view->display($template, $data) показывает этот шаблон
делает эхо

в контроллерах не принято вызывать эти методы (в отличии от ИИ)
тк ___ контроллер не управляет представлением ___
задача контроллера узнать что хочет пользователь,  получить данные, обратиться к модели



